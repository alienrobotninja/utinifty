import Header from "../components/Header";
import Form from "../components/Form";

const Minter = () => {
    return (
        <div>
            <Header />
            <Form />
        </div>
    );
}

export default Minter;